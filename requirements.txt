# AI Content Creator — Personal Brand Engine
# Ironhack AI Engineering · Module 2 Project
#
# Install: pip install -r requirements.txt
# Requires Python 3.10+

# ── LLM & API ─────────────────────────────────────────────────────────────────
openai>=1.30.0

# ── Web UI ────────────────────────────────────────────────────────────────────
gradio>=4.0.0

# ── HTTP requests (HN Algolia, Dev.to, TechCrunch RSS, Trello API) ────────────
requests>=2.31.0

# ── Environment / secrets ─────────────────────────────────────────────────────
python-dotenv>=1.0.0

# ── Content quality analysis (content_analyzer.py) ───────────────────────────
# TF-IDF cosine similarity for uniqueness scoring vs generic AI output.
# Flesch readability is computed manually — no textstat needed.
scikit-learn>=1.3.0

# ── Standard library (no install needed) ─────────────────────────────────────
# pathlib, logging, json, re, math, time, datetime, typing, os, sys
